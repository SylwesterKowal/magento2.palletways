<h3><?php echo __('Create a Consignment') ?></h3>
<?php
$response = json_decode($block->getCreateConsignment());


if ($response->Status->Code == 'OK' || $response->Status->Description == 'Successful') {
    $block->getRedirectPalletways('https://portal.palletways.com/login_v2.php', 'en');
} else {
    var_dump($block->getCreateConsignment());
}
